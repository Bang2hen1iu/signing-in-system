# spring datasource
spring.datasource.url=jdbc:postgresql://localhost:5432/signing_sys_test
spring.datasource.username=postgres
spring.datasource.password=159951159
spring.jpa.show-sql=true

# Hibernate ddl auto (create, create-drop, update): with "update" the database
# schema will be automatically updated accordingly to java entities found in
# the project
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

flyway.enabled=true

server.port=8080

spring.jersey.application-path=/api

admin.cookie.token.name=token
admin.cookie.token.age=604800
super-admin.cookie.token.age=1800

course.start_date=2016-08-28

task_execute_time=1

signatureImgPath=signature/

spring.mvc.favicon.enabled=false